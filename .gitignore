# Working files for Claude (planning artifacts, specs, memory,
# per-user settings).  Nothing under .claude/ is intended to be
# committed.
.claude/

# Go build artifacts
coverage.out
coverage.html
*.test
*.out

# Editor
.vscode/
.idea/
.DS_Store

# MkDocs build output (repository root only, so a nested directory named
# "site" is still tracked).
/site/
